# v3改良設計書 — 昇格見送りを受けたROI対応 (2026-09-13)

## 0. 背景

v3シャドウテスト(2026-09-04〜09-12、1,307R)の昇格判定は**見送り**。
基準①(1着的中率 v3 57.2% > v2 56.8%)・基準②(オフライン推定±3pt以内)は達成したが、
基準③(4戦略ROI非悪化)が未達成。一撃重視で -48.7pt の大幅悪化。

原因: v3は精度向上の代償として予測が本命(1号艇)側へ極端に寄り、
高配当を狙う戦略設計と衝突した。

本書は (1) ROI指標CVの設計、(2) 重み付き学習の設計と理論的妥当性、
(3) 実データシミュレーションによる効果検証、(4) ハイブリッド案の評価、
(5) 推奨方針 をまとめる。**実装・DB書き込みは未実施。**

---

## 1. 原因の定量確認 — 「本命寄り化」の実測

walk-forward 3fold検証期間(2026-07-27〜09-02、4,625R)での
「1号艇をrank1に置いた率」(本命寄り度):

| モデル | top1的中率 | 1号艇をrank1にした率 |
|---|---|---|
| v2現行 | 49.3% | 64.5% |
| v3現行 | 54.9% | **91.7%** |
| (実際の1号艇1着率) | ─ | 約54% |

v3は9割以上のレースで1号艇を1位予測にしており、実際の1号艇勝率(54%)を
大幅に超える過剰な本命依存。的中率最大化の解として「迷ったら1号艇」に
収束した形。これが高配当帯の的中消失 → ROI悪化の直接原因。

オフライン再現(kpi_offline_v3.py、4,625R)でもシャドウと同じ構図:

| 戦略 | v2 的中/ROI | v3 的中/ROI | ROI差 |
|---|---|---|---|
| 的中特化 | 21.9% / 85.7% | 24.2% / 83.6% | -2.1pt |
| バランス | 38.2% / 79.3% | 42.0% / 74.7% | -4.6pt |
| 一撃重視 | 11.5% / 97.9% | 12.1% / 88.3% | **-9.6pt** |
| 絞り込み | 8.6% / 87.5% | 9.2% / 87.6% | +0.1pt |

※シャドウ期間の「一撃重視 v2=119.1%」はオフラインの97.9%より大幅に高く、
9日間の上振れ(高配当的中の運)を含む。ROIは少数の大的中に支配されるため
短期間の点推定は不安定(→§2のCI設計につながる)。

---

## 2. ROI指標クロスバリデーションの設計 (run_lr_v3.py組み込み)

### 2.1 何を測るか

**主指標: fold別・戦略別ROI(現行本番の買い目生成ロジックをそのまま再現)**

- 対数損失の重み付けや期待値近似ではなく、**実際の4戦略シミュレーションROI**を使う。
  理由: 昇格基準③そのものであり、買い目生成のオッズフィルター
  (バランス≤100倍、一撃≥15倍)がモデルのランク分布と相互作用するため、
  代理指標では捕捉できない(今回の見落としがまさにこれ)。
- 補助指標: top1的中率、AUC、**1号艇rank1率**(本命寄り度の早期警報。
  実際の1号艇勝率+10pt超なら過剰本命化のシグナル)。

### 2.2 実装形

- kpi_offline_v3.py の戦略再現ロジック(strategies_for / payout_for)を
  共通モジュール `strategy_sim.py` に切り出し、run_lr_v3.py のfoldループから呼ぶ。
- 必要データ: fold検証期間のオッズ・払戻・着順CSV
  (export_odds_payouts_v3.php で出力済みの odds_/payouts_/finish_*.csv)。
- 出力: fold別・戦略別の 的中率/ROI を候補モデルとv2ベースラインで併記。

### 2.3 昇格ゲートの定義(点推定比較からの脱却)

ROIはレース単位ブートストラップ(5,000回)でCIを付ける。実測のノイズ幅:

```
一撃重視ROI差 v2−v3     : +9.7pt  95%CI [−3.1, +23.7]  P(v3が同等以上)=0.074
一撃重視ROI差 v2−v3w05  : +5.5pt  95%CI [−8.6, +20.0]  P(v3w05が同等以上)=0.224
```

4,600レースでもROI差±10pt程度はCIが跨る。よってゲートは:

- **必須(hard gate)**: 全戦略で P(候補モデルのROIがv2以上) ≥ 0.25
  (「悪化がほぼ確実」なモデルを弾く。現行v3の一撃0.074は不合格)
- **警告(soft gate)**: ROI点推定が−3pt超の戦略は要注記
- **本命寄り度**: 1号艇rank1率が実勝率+15pt超なら不合格(構造的な歪みの検出)

> **2026-09-13改訂(実装時判断)**: 本命寄り度はhard→soft警告に降格した。
> 実装後のグリッドサーチで、ROI 4ゲートを通過する全candidate(alpha=0.4/0.55/0.6)が
> 本命寄り度15pt基準に抵触し(通過はalpha=0.7のみだが絞り込みROIが崩壊)、
> 両立点が存在しないことが判明。ROI実害はhard gateが直接測定しており二重計上で
> あること、v2自身も実勝率+11.7ptと15pt基準にほぼ抵触することから、警告のみとした。
> あわせてグリッドサーチの結果 **alpha=0.55を採用**(全戦略P≥0.33でノイズ耐性最良、
> バランスROI v2超え、top1 55.4%)。詳細は v3w055_training_output.txt を参照。

---

## 3. 重み付き学習の設計と理論的妥当性

### 3.1 設計

勝者サンプル(is_winner=1)の学習重みを枠番prior勝率の逆数ベキで増幅:

```
w_i = min(12, (1 / prior1[lane_i]) ^ alpha)   # 勝者のみ。敗者は w=1
```

- prior1[lane] = trainデータの枠番別1着率(1号艇≈54%…6号艇≈3%)
- 単勝オッズがデータに無いため、市場確率の代理として枠番priorを使用
  (枠番は配当と最も強く相関する変数)
- cap=12 で6号艇(逆数≈32)の過大重みをクリップ

### 3.2 理論的妥当性

- 通常のロジスティック回帰MLEは**較正された確率**を目指すため、ランキングは
  「最も当たりやすい艇」= 本命に収束する。これは的中率には最適だがROIには無関係。
- 賭けの対数資産成長(Kelly基準)は Σ p_i·log(odds_i) に比例し、
  odds_i ≈ 1/市場確率。勝者の対数尤度に w ∝ (1/市場確率)^alpha を掛けることは、
  **「配当加重の的中率」を最適化する重要度重み付け**に相当する。
  alpha=0が純粋精度、alpha=1が期待対数リターン寄り、その間で精度とROIを内挿。
- 副作用: 出力確率の較正は崩れる(穴側の確率が過大評価される)。
  戦略生成はランク順のみ使用するため影響なし。ただし画面表示の
  win_probability にそのまま使うと数値が歪む点は昇格時に要対応
  (表示用にはレース内正規化があるため実害は限定的だが、較正済みv3と
  確率値の意味が変わることは明記しておく)。

### 3.3 「精度が上がると高配当を外す」問題は緩和できるか

実測で**部分的に緩和できた**(§4)。alpha=0.5で本命寄り度が91.7%→77.0%に
低下しつつ、top1的中率はむしろ向上(54.9%→55.5%)。トレードオフの
パレートフロンティア上でより良い点に移動した形。
ただし一撃重視ROIはv2の点推定には届かず、**トレードオフの完全な解消ではない**。

---

## 4. シミュレーション結果 (2026-07-27〜09-02、4,625R、walk-forward 3fold)

比較: v2現行 / v3現行 / v3w05(重み付きalpha=0.5) / v3w10(alpha=1.0) /
blend(v2・v3確率の幾何平均)

### 4.1 精度と本命寄り度

| モデル | top1的中率 | 1号艇rank1率 |
|---|---|---|
| v2 | 49.3% | 64.5% |
| v3 | 54.9% | 91.7% |
| **v3w05** | **55.5%** | 77.0% |
| v3w10 | 51.1% | 55.7% |
| blend | 55.0% | 83.6% |

alpha=0.5が精度・本命寄り度ともに最良バランス。alpha=1.0は重みが強すぎ
精度が崩れる(51.1%)。

### 4.2 4戦略 的中率/ROI

| 戦略 | v2 | v3 | v3w05 | v3w10 | blend |
|---|---|---|---|---|---|
| 的中特化 | 21.9/85.7 | 24.2/83.6 | 24.7/**86.0** | 23.5/86.2 | 23.9/**87.2** |
| バランス | 38.2/**79.3** | 42.0/74.7 | 42.7/77.9 | 40.6/**79.7** | 41.1/77.8 |
| 一撃重視 | 11.5/**97.9** | 12.1/88.3 | 12.0/92.5 | 11.9/90.3 | 12.5/92.6 |
| 絞り込み | 8.6/87.5 | 9.2/**87.6** | 9.2/85.9 | 8.8/86.2 | 9.0/86.2 |

- v3w05は現行v3に対し全主要戦略でROI改善(+2.4〜+4.2pt)、的中率も維持〜向上
- 的中特化はv3w05でv2をROI・的中率とも上回る(86.0≥85.7、+2.8pt的中)
- 一撃重視はどの改良でもv2の97.9%に届かない(最良でblend/v3w05の92.5前後。
  ただし差はノイズ帯域内、§2.3のCI参照)

### 4.3 fold別 一撃重視ROI(安定性)

| fold | v2 | v3 | v3w05 | blend |
|---|---|---|---|---|
| fold1 (7/27-8/9) | 103.4% | 78.7% | 90.7% | 88.6% |
| fold2 (8/10-8/23) | 92.3% | 95.2% | 93.6% | 91.3% |
| fold3 (8/24-9/2) | 100.3% | 89.3% | 93.1% | 101.9% |

v2の優位はfold1・3に集中し、fold2ではv3系が上。v3w05は3fold全てで90%台と
**分散が小さい**(v2: 92-103%、v3: 79-95%)。

---

## 5. ハイブリッド案の評価 (一撃・絞り込み=v2 / 的中特化・バランス=v3系)

### 5.1 効果

| 構成 | 的中特化 | バランス | 一撃重視 | 絞り込み | 基準③判定 |
|---|---|---|---|---|---|
| v2単独(現行) | 85.7 | 79.3 | 97.9 | 87.5 | ─ |
| v3単独 | 83.6 | 74.7 | 88.3 | 87.6 | ✗✗✗ |
| ハイブリッド(v3+v2) | 83.6 | 74.7 | 97.9 | 87.5 | ✗(的中特化・バランス) |
| **ハイブリッド(v3w05+v2)** | **86.0** | 77.9 | **97.9** | **87.5** | △(バランス−1.4ptのみ) |

現行v3とのハイブリッドでは的中特化・バランスのROI悪化が残るため不十分。
**v3w05とのハイブリッド**なら、悪化はバランスの−1.4pt(ノイズ帯域内)のみで、
的中率は的中特化+2.8pt・バランス+4.5ptの改善を獲得できる。

### 5.2 実装コスト・保守性

- **インフラは既存**: predictions_v2テーブル+api_v3_shadow.phpのシャドウ経路を
  そのまま第2モデル本番経路に転用可能。追加テーブル不要。
- **改修箇所**: generate_strategies.php が戦略別に参照テーブルを切り替える
  (的中特化・バランス→predictions_v2、一撃・絞り込み→predictions)。
  切替は戦略タイプ→テーブルのマップ1つで済み、改修は小規模。
- **保守コスト(デメリット)**:
  - 再学習・監視対象が2モデルになる(v2は係数固定なので実質+1)
  - 予測画面の表示順位(v2)と的中特化の買い目根拠(v3w05)が食い違う
    UX不整合が発生。表示をどちらに寄せるかの製品判断が必要
  - 昇格判定・KPI集計が戦略×モデルの2次元になり運用が複雑化
- 単一モデル改良(v3w05のみで全戦略)と比べ、ROI保全効果は大きいが
  複雑性コストが恒常的にかかる。

---

## 6. 推奨方針

**推奨: 「v2継続 → v3w05を新シャドウ候補に差し替え → 段階的ハイブリッド昇格」**

1. **本番は当面v2継続**(即時変更なし)。
2. **ROI-CVゲートをrun_lr_v3.pyに実装**(§2)。以後の全候補モデルは
   戦略別ROI+ブートストラップCI+本命寄り度チェックを通過しないと
   シャドウにも進めない。現行v3はこのゲートで学習段階に弾けていた。
3. **v3w05(重み付きalpha=0.5)を再学習し、シャドウをv3→v3w05に差し替え**。
   predictions_v2への書き込みモデルを入れ替えるだけで既存の
   shadow_eval_v3.phpがそのまま使える。期間は2週間(ROIのノイズ幅を
   考慮し、今回の9日より長く)。
4. シャドウで基準③(CI版)をクリアしたら**段階的に昇格**:
   - 第1段: 的中特化のみv3w05へ切替(オフラインでROI・的中率ともv2超えの
     唯一の戦略。リスク最小)
   - 第2段: バランスを2週間の実績確認後に判断
   - 一撃重視・絞り込みは**v2維持**(v2の一撃優位は点推定+5.5ptで、
     覆す根拠が出るまで現状維持が合理的)
5. alpha(0.3〜0.7)とcapのグリッドサーチはROI-CVゲート実装後に実施
   (今回は0.5/1.0の2点のみ。0.5近傍にさらに良い点がある可能性)。

### 採らなかった選択肢と理由

- **v3(現行)単一昇格**: 基準③未達が確定的(P=0.074)。却下。
- **v3w05単一昇格(全戦略)**: 一撃重視ROIが点推定で−5.5pt。ノイズ帯域内とはいえ
  黒字に最も近い戦略を弱いモデルに任せるリスクは取らない。
- **v2完全継続(v3廃棄)**: 的中率+5〜6pt相当の改善(top1 49.3→55.5)を捨てるのは
  もったいない。的中率はユーザー向け訴求の中核指標であり、ROIを守りながら
  取り込む価値がある。
- **alpha=1.0**: 精度崩壊(51.1%)でROIも改善せず。重み過剰。

---

## 付録: 使用スクリプト(一時・DB書き込みなし)

- `sim_v3_improved.py` — 全変種の4戦略シミュレーション
- `sim_v3_bootstrap.py` — 一撃重視ROI差のブートストラップCI
- 入力: lr_data_v3_full.csv, odds_/payouts_/finish_2026-*.csv (ローカル既存)
