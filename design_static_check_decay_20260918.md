# 静的チェックの形骸化 — 「通っているチェック」が品質を保証しなかった事例

作成日: 2026-09-18
関連: `tests/StaticChecksTest.php`, `app.js`, `home-races.js`, `predict.php`, `my-picks.php`

## 1. 概要

本プロジェクトでは、過去に多発した「自ドメイン向けの絶対URLを `fetch()` に直書きしてしまう」
バグ（2026-09-10 に10ファイルで修正）の再発防止として、CIに**静的チェック**
（`StaticChecksTest`）を組み込んでいた。しかしこの検査は、後述の2つの欠陥により
**実際にはほとんど機能しておらず**、同種の絶対URLが計6箇所以上、長期間にわたって
見逃されていた。CIは常にグリーンだったため、問題は誰にも気付かれなかった。

この文書は「チェックが通っている＝品質が保証されている、ではない」という教訓を、
具体的な失敗の解剖として記録するものである。

## 2. 何が起きていたか

### 2.1 許可リストに自ドメインが登録され、検査が無効化されていた

`StaticChecksTest` の絶対URL fetch 検出は、検出したホストを許可リスト
`ALLOWED_ABSOLUTE_FETCH_HOSTS` と照合し、載っていれば違反としない仕組みだった。
ところがこの許可リストに、**アプリ自身の本番ドメイン `2410049.moo.jp`** が
「外部の会場情報API（自ドメインではない正当な外部呼び出し）」というコメント付きで
登録されていた。実際には `2410049.moo.jp` はアプリの本番ホストそのもの（同一オリジン）
であり、そこへの `fetch` は相対パスで書くべきものである。

結果として、`app.js` の

```js
var res = await fetch('https://2410049.moo.jp/venues.php?date=' + window.PAGE_DATE);
```

は正規表現では正しくホストを抽出できていたにもかかわらず、**許可リストに載っている
ホストだったため違反と判定されず**、検査をすり抜けていた。検査ロジックの正誤ではなく、
「自ドメインを外部APIと誤ってラベル付けして例外登録した」という設定ミスが原因である。

### 2.2 検出対象が `fetch('literal')` の直書きに限られていた

検出は `fetch(` の直後にクォート付きの絶対URL文字列が続く形（`fetch('https://...')`）
だけを対象にしていた。このため、**ベース変数経由**や**文字列結合経由**で組み立てられた
絶対URLは構造的に検出できなかった。

- `home-races.js`: `var API_HOST = 'https://2410049.moo.jp';` を定義し、
  `fetch(API_HOST + '/api_predict.php...')` の形で使用（複数箇所）。
- `predict.php`: 後述のとおり文字列を分割して代入。

`fetch()` の引数が変数（`fetch(url)` / `fetch(API_HOST + ...)`）になった時点で、
文字列リテラル前提の正規表現は無力だった。

### 2.3 検出を「意図的に」回避するコードが書かれていた

最も問題だったのは `predict.php` の以下のコードである。

```js
// ホスト名を文字列結合にするのはCSPスキャナによる直書き検出を避けるため
var API_BASE = 'https://' + '2410049.moo.jp';
```

コメントが明言しているとおり、これは**静的検査に引っかからないよう、URLを
`'https://' + '2410049.moo.jp'` と意図的に分割して書いたもの**である。
検査を通すために問題（自ドメイン絶対URL）を修正するのではなく、
検査に見つからないようコードを書き換える、という本末転倒が起きていた。
検査機構が「回避すべき障害物」として扱われた瞬間、そのチェックは品質保証の
役割を完全に失っていた。

### 2.4 見逃されていた箇所（計6箇所以上）

| ファイル | 箇所 | 形態 | 見逃し理由 |
|---|---|---|---|
| `app.js` | fetch 1箇所 | 直書きリテラル | 許可リストに自ドメイン登録（2.1） |
| `home-races.js` | ベース変数定義1 + fetch 3箇所 | ベース変数経由 | 変数経由で検出対象外（2.2） |
| `predict.php` | ベース変数定義1 + fetch 2箇所 | 文字列分割 | 意図的回避（2.3） |
| `my-picks.php` | アンカー `href` 1箇所 | fetch以外 | そもそも fetch のみが検査対象 |

`fetch` として少なくとも6箇所（app.js×1・home-races.js×3・predict.php×2）が
自ドメイン絶対URLのまま見逃されていた（変数定義やリンクhrefを含めるとさらに多い）。
なお `admin_v2.php` にも自ドメイン絶対URLがあるが、これは**サーバー側 PHP の
`curl_init`** 用であり、curl は絶対URLが必須のため正当な使用として除外している。

## 3. 修正内容

`tests/StaticChecksTest.php` と対象ファイルに対して以下を実施した。

1. **許可リストから自ドメインを除去**。許可リストは真に外部のホスト
   （`api.github.com` のみ）に限定し、「自ドメインは同一オリジンなので相対パスで
   書くこと。ここに本番ホストを追加してはならない」旨をコメントで明記した。
2. **fetch検出の正規表現をバッククォート対応に拡張**（テンプレートリテラルによる
   `fetch(\`https://...\`)` も検出できるように）。
3. **クライアントJSへの本番ホスト出現そのものを禁止する検査を追加**。
   `.js` ファイルと `.php` 内の `<script>` ブロックを対象に、本番ホスト文字列
   （`2410049.moo.jp`）が現れたら違反とする。これによりベース変数経由・文字列分割
   経由の回避（2.2 / 2.3）を封じた。サーバー側PHPの curl 等は `<script>` 外なので
   対象外のまま。
4. **header.php include検査を自動検出方式に変更**。従来はチェック対象ページ名を
   ハードコードした一覧を回していたため「新規ページを一覧に追加し忘れる」と検査から
   漏れる構造だった。`<!DOCTYPE` / `<html>` を出力するルート直下ページを自動抽出して
   検査するよう改め、一覧のメンテ漏れという同型の穴も塞いだ。
5. 見逃されていた `app.js` / `home-races.js` / `predict.php` / `my-picks.php` の
   自ドメイン絶対URLをすべて相対パス（同一オリジン）に修正した。

修正後、リポジトリ全体を再スキャンし、クライアント側の自ドメイン絶対URLが
残っていないこと、header.php の include 漏れが無いことを確認した。

## 4. 教訓

- **検査機構そのものを検証しない限り、「チェックが通っている」ことは品質の保証にならない。**
  今回、CIは一度も赤くならなかったが、それは品質が保たれていたからではなく、
  検査が対象を取りこぼしていた（＝そもそも仕事をしていなかった）からだった。
  グリーンは「問題が無いこと」ではなく「検査が問題を報告しなかったこと」しか意味しない。
- **許可リスト・例外設定は検査の穴になりやすい。** 例外を1つ足すたびに検査の網は緩む。
  特に「自ドメインを外部として例外登録する」ような、前提を誤ったコメント付きの例外は、
  一見もっともらしく見えるため長く放置される。例外には「なぜ安全か」を検証可能な形で
  残し、定期的に棚卸しすべきである。
- **検査を回避するコードは、バグそのものより危険な兆候である。** `predict.php` の
  「スキャナ回避」コメントは、検査が「守るべき基準」ではなく「避けるべき障害」として
  扱われていたことを示す。回避が可能な検査は、いずれ回避される。検査は回避コストより
  修正コストのほうが低くなるよう、逃げ道を塞いだ実装にする必要がある。
- **再発防止策そのものにも寿命と検証が要る。** 一度書いた静的チェックは放置すると
  形骸化する。検査が「本当に対象を捕まえられるか」を、既知の違反サンプルで
  定期的に確かめる（＝検査のテスト）ことが望ましい。
