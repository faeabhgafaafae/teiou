# v3モデル詳細設計書 (P1-P3: 再学習+特徴量追加+枠番one-hot化)

作成日: 2026-09-03 / 前提: model_improvement_report_20260903.md のP1-P3
ステータス: **設計段階(コード実装・DB書き込み未実施)**

---

## 0. 設計中に発見した重要事項(先に共有)

**現行v2の学習データにはlocal_win_rateの先読みリーク(look-ahead bias)がある。**
旧export_lr_data.php(git b4b30d3^)の当地勝率集計は「現在から過去2年」の全resultsを集計しており、**レース日より後の結果も含んでいた**(例: 7/1のレースの特徴量に7/2〜7/28の当地成績が混入)。本番推論時(api_predict.php)は当然未来データが存在しないため、学習と推論で分布がズレている。local_win_rateの係数が+0.975と突出しているのはこのリークで増幅されている可能性がある。

→ v3のexportでは**集計上限を「レース日前日」に修正**する(§2.4)。修正によりオフラインAUCは見かけ上下がる可能性があるが、本番の実力との乖離が減り、確率のキャリブレーション(EVフィルタの前提)が改善する。

---

## 1. 特徴量詳細設計

### 1.1 現行11特徴量の扱い

| 現行 | v3での扱い | 理由 |
|---|---|---|
| lane | **one-hot化(§1.6)** | 非線形な枠番効果を正確に表現 |
| global_win_rate | 維持 | |
| global_2rate | **削除** | 係数-0.102で符号逆転(勝率と多重共線) |
| local_win_rate | 維持(リーク修正) | §0 |
| local_2rate | **削除** | 係数-0.018で死んでいる |
| exhibit_time_rel | 維持 | |
| start_timing(展示ST) | 維持 | |
| motor_2rate_rel | 維持 | |
| wind_speed / wave_height | 維持 | 害はなくコストゼロ。交互作用は将来課題 |
| temperature | **削除** | 係数-0.0001 |

### 1.2 avg_st(期別平均ST) — player_periods.avg_st

- 2特徴量を追加:
  - `avg_st`: 生値(約0.11〜0.20秒)。標準化はscaler任せ
  - `avg_st_rank`: レース内順位1〜6(1=最速)。スタート力は相対比較が本質のため
- 欠損(新人等): レース内中央値→全体中央値で補完。欠損率は低い見込み
- 取得: 既存の最新期JOINに列追加のみ

### 1.3 コース別成績 — player_periods.c{lane}_rank1 / c{lane}_count / c{lane}_fukusho

- その選手の「当該枠番での」1着率。v1では使っていたがv2で欠落した、枠番×選手の交互作用そのもの
- `course_win_rate_sm` = (c{lane}_rank1 + k·prior[lane]) / (c{lane}_count + k)
  - ベイズ平滑化。少数走(0/3=0%や1/2=50%)のノイズを枠番全国平均に收縮
  - prior[lane] = 学習データ実測の枠番別1着率(≈ 1:0.55, 2:0.14, 3:0.12, 4:0.11, 5:0.06, 6:0.02。exportで実算)
  - k ∈ {5, 10, 20} をCVで選択
- `course_fukusho_sm`: c{lane}_fukushoを同様にc{lane}_countで収縮。効果はablationで判断し、寄与なければ落とす
- 注意: 枠番=進入コースの近似(枠なり崩れ≈5%は無視)。exhibit_course対応は将来課題
- exportでは生値(rank1/count/fukusho)で出力し、平滑化はPython側で実施(kチューニングのため)

### 1.4 直近10走トレンド — resultsから集計

- 対象: 当該選手のresults行で、
  - `races.date < 当該レース日`(**同日は除外** — 同日内のレース順はscheduled_timeでしか決まらず、順序誤りによる同日リークを防ぐ)
  - `actual_rank IS NOT NULL`(欠場・事故失格でランクなしはスキップ)
  - 遡り上限180日、直近から最大10走
- 特徴量(3つ):
  - `recent10_avg_rank`: 平均着順。**3走未満はNULL**→中央値補完
  - `recent10_win_rate`: 1着率
  - `recent10_st_mean`: 実戦ST平均(results.start_timing、NULL除外。展示STより実戦の方が実力を反映)
- 実装(export側): 対象期間+遡り180日分のresultsを選手単位でまとめて取得し、PHP側で日付ソート済み配列をポインタ走査(選手×レース日ごとに直近10件を切り出し)。1ヶ月チャンクなら選手約1,600人×約40走=6.4万行程度でメモリ安全圏
- 実装(本番api_predict側): レースの6選手をIN句1本で取得(player_id索引あり、負荷軽微)

### 1.5 grade(級別)

- ソースは`players.grade`(現在値)ではなく**`player_periods.grade`(期別値)**を使う。players.gradeは「今の級」であり、過去レースの学習時にリークになるため(§2.4のas-of原則と統一)
- one-hot 3ダミー: `grade_A1`, `grade_A2`, `grade_B1`(基準=B2)

### 1.6 枠番one-hot化

- **現状**: lane∈{1..6}を連続値1本で表現、係数-1.133。「1枠→2枠」と「5枠→6枠」の勝率落差を同じ傾きで近似しており、実際の急減衰(1号艇≈55%→2号艇≈14%→…)に対して1号艇過小・中枠歪みが生じる
- **変更**: `lane_2`〜`lane_6`の5ダミー(1号艇=基準、切片に吸収)。各枠が独立のオフセットを持ち、任意の減衰形状を表現できる
- PHP側: score_race()の特徴量ベクトル構築で lane→5個の0/1 に展開(§3.3)

### 1.7 v3特徴量リスト(21個)

```
lane_2..lane_6                    (5)  one-hot
global_win_rate, local_win_rate   (2)
grade_A1, grade_A2, grade_B1      (3)  one-hot
avg_st, avg_st_rank               (2)
course_win_rate_sm, course_fukusho_sm (2)
recent10_avg_rank, recent10_win_rate, recent10_st_mean (3)
exhibit_time_rel, start_timing, motor_2rate_rel (3)
wind_speed, wave_height           (2)
```

学習行数≈5万に対し21特徴量のLRは十分小さい(過学習リスク低)。
**追加は段階評価**: ①one-hot+期間拡大のみ → ②+ST系 → ③+コース別 → ④+直近トレンド+grade、と積んでablation(各段のAUC/的中率寄与)を記録し、寄与のない群は落とす。

---

## 2. 学習データ期間の拡大

### 2.1 期間と規模

- **2026-06-29 〜 実行日-2日**(約9.5週)。開始日はbeforeinfo(展示タイム/ST)取得が安定した日で、現行と同じ。それ以前はexhibit_time欠損でREQUIRED除外されるため遡っても増えない
- 推定: 約60営業日 × 約140レース/日 × 6行 ≈ **5万行**(現行の約3倍)

### 2.2 分割方法: walk-forward 3-fold + 最終全量fit

```
fold1: train 6/29〜7/26 → val 7/27〜8/9
fold2: train 6/29〜8/9  → val 8/10〜8/23
fold3: train 6/29〜8/23 → val 8/24〜最新
```

- 特徴量選択・平滑化k・正則化Cはfold平均のAUC/レース的中率で決定(単一分割の偶然に依存しない)
- 決定後、**全期間で再fitした係数を本番用に出力**
- 現行run_lr.pyの80/20時系列分割も互換のため残す

### 2.3 jcdバグ期間(6/17〜7/5、10会場)の再確認結果

結論: **専用の除外処理は不要。既存JOINで自動除外が期間拡大後も機能する。** ただし検品チェックは入れる。

根拠(コミット6d98807・8/7調査エンドポイント345de19を再確認):
- バグ内容: scrape側VENUES辞書のjcd≥15が1つズレ(架空「高松」挿入が原因)。**汚染されたのはentries(出走表・直前情報)のみ**。丸亀/児島/宮島/徳山/下関/若松/芦屋/福岡/唐津は隣会場のデータが誤ラベル保存、大村は保存自体されず。7/6修正、汚染行の遡及削除は未実施
- **resultsは無影響**(download_results.pyは会場名ベース正規化 — 修正コミットに明記)
- 学習データ抽出は `JOIN results res ON res.race_id = e.race_id AND res.player_id = e.player_id` を維持 → 誤ラベルentriesの選手は同レースの正しいresultsに存在せずJOIN不成立で自動除外。選手は同日1会場しか出走しないため偶然一致は実質ゼロ。**この構造は期間を拡大しても変わらない**
- 派生特徴量への波及: 当地勝率・直近10走・実戦STは全て**results由来なので汚染なし**。player_periodsはファンデータ由来で無関係
- 影響は「6/29〜7/5の10会場分(約700〜800レース)が学習から欠落する」ことのみ。拡大後データの約8%で許容範囲
- **検品チェック(実装に含める)**: export後にレース数の日別×会場別分布を出力し、(a)バグ期間×影響10会場が0件であること、(b)7/6以降は全24場が揃うことを目視確認する手順を踏む

### 2.4 リーク防御(as-of原則)

| データ | 現行の問題 | v3設計 |
|---|---|---|
| local_win_rate | **レース日以降の結果も混入(§0)** | 集計を`rc.date < レース日`かつ2年窓に修正 |
| player_periods(勝率・avg_st・grade・コース別) | 現在は実質問題なし(最新期=2026年4月版ファンデータ、集計期間が〜4月のため6月以降のレースにリークしない) | 「レース日時点で公開済みの最新期」を選ぶas-of JOINにする。**10月の新ファンデータ投入後の再学習で必須になる**ため今回から実装 |
| 直近10走 | (新規) | レース当日を除外した過去日のみ(§1.4) |
| 展示タイム・展示ST・モーター・気象 | 問題なし(当日レース前情報) | 現行維持 |

---

## 3. 実装の全体設計

### 3.1 データ抽出: export_lr_data_v3.php(新規・読み取り専用)

旧export_lr_data.php(git `b4b30d3^`から復元)をベースに:

1. **チャンク対応**: `?from=YYYY-MM-DD&to=YYYY-MM-DD`で期間指定出力。60日一括はホスティングのメモリ制限で落ちる(simulate_balanceの60日失敗で実証済み)ため、**2週間チャンク×5回**をローカルからcurlで取得しPython側で結合
2. 追加カラム: `avg_st, grade_period, course_rank1, course_count, course_fukusho, recent10_avg_rank, recent10_win_rate, recent10_st_mean, recent10_count`
3. リーク修正: local集計に`rc.date < レース日`上限、player_periodsのas-of JOIN
4. 検品用に`?stats=1`で日別×会場別レース数のJSONを返すモード(§2.3のチェック用)
5. api_key認証。検証完了後は削除でも残置でも可(月次再学習で使うなら残置+定期メンテ)

### 3.2 学習: run_lr.py改修(run_lr_v3.pyとして新規)

- one-hot生成(lane/grade)、ベイズ平滑化(k grid)、walk-forward CV、ablationレポート
- **ベースライン同梱**: 現行v2の係数(predict_v2_core.phpの定数)をPython側に持ち、同一validation期間でv2 vs v3を同条件評価
- 評価指標: 行単位AUC/LogLoss、レース単位1着的中率、**上位3艇3着内完全一致率(的中特化相当)**、上位2艇1着率(バランス1着固定相当)
- `--php`でPredictV3用のPHP定数一式を出力(特徴量名コメント付き)

### 3.3 本番反映: PredictV3クラス新設(predict_v3_core.php)

- **PredictV2の書き換えではなく新クラス**を推奨:
  - 切り戻しがapi_predict.php内のrequire1行で済む
  - シャドウ運用時にv2本番/v3シャドウを同居できる
- score_race()の変更点: 特徴量ベクトルがlaneダミー5+gradeダミー3を含む形に。入力配列に`avg_st, grade, course_rank1, course_count, course_fukusho, recent10_*`を追加
- api_predict.php側の追加取得:
  - player_periodsクエリ: `avg_st, grade, c1〜c6のrank1/count/fukusho`を**既存クエリの列追加**で取得(クエリ本数増なし)
  - 直近10走: 6選手まとめてIN句1本の新クエリ(player_id索引あり)。1レースあたり+1クエリで負荷軽微。実測で問題が出た場合のみ日次事前集計テーブルを検討(今回はやらない)
- 平滑化定数(prior[lane], k)はPHP定数として焼き込み

### 3.4 predictionsテーブル / model_version

- **新識別子`v3_lr`を振る**(model_versionカラムは既存、ALTER不要)
- 過去行は書き換えない → model_versionで層別すればv1/v2/v3の期間別成績比較がSQL一発で可能(今回のバランス検証で層別が役立った実績)
- **predictions_v2テーブルはv3シャドウ用に転用**(8/27のv2昇格以降、書き込み停止中で空き。テーブル名は実態とズレるが、シャドウ完了後に用済みになる一時用途なので新テーブル追加より低コスト)。win_probabilityカラムがsimulate_balance.phpのEV計算にそのまま使える点も好都合

---

## 4. 検証計画

### 4.1 オフライン検証(実装直後)

1. **モデル指標**: 同一validation期間でv2係数 vs v3 — AUC/LogLoss/レース単位的中率(§3.2)
2. **戦略KPIへの落とし込み**: v3順位で直近30日の4戦略(的中特化/バランス/一撃/絞り込み)の的中率・回収率をPythonで再計算(オッズ・払戻はexportに含めるかsimulate系エンドポイントで取得)。**モデル指標の改善が戦略KPIに転写されることを本番前に確認**(AUCが上がってもKPIが上がらないなら昇格しない)

### 4.2 シャドウテスト: **実施する(ただし1週間の短縮版)**

実施すべき理由:
- 新特徴量(直近10走・avg_st・コース別)は**本番のデータ揃い方・欠損パターンに依存**する。オフラインCSVは「揃った後のDB」だが、本番は朝7時時点の状態で走る。このリアルタイム経路の差はオフラインでは検証できない
- §0のリーク修正により「オフライン指標と本番実力の乖離」を今回初めて正しく測れる。シャドウはその答え合わせ
- v2昇格時(1ヶ月シャドウ)の成功実績と同じ手順を踏むのが安全

短縮できる理由: モデル形式は同じLR系で変更は特徴量のみ。オフライン検証(walk-forward+戦略KPI)が厚い。→ **1週間・約1,000レースで判定**

- 方法: `api_v3_shadow.php`(api_v2_batch.phpの改修版)を日次Actionsに追加し、predictions_v2テーブルへ保存。本番predictions(v2)には触れない
- 昇格判定基準(3つすべて):
  1. シャドウ1着的中率がv2同期間実績を上回る
  2. シャドウ1着的中率がオフライン推定値の-3pt以内(乖離が大きい場合は実装バグかリーク残りを疑う)
  3. v3順位での戦略シミュレーション(4戦略)が現行実績を悪化させない
- 昇格作業: api_predict.phpのrequireをPredictV3に切り替え(v2昇格8/27と同一手順)。切り戻しも1行

---

## 5. スケジュール感

| 日程 | 作業 | 成果物 |
|---|---|---|
| Day 1 | export_lr_data_v3.php実装→デプロイ→チャンクDL→検品(jcdバグ0件確認・分布確認) | 学習CSV(約5万行) |
| Day 2 | run_lr_v3.py実装、walk-forward CV、ablation、k/Cチューニング | 係数+ablationレポート |
| Day 3 | 戦略KPIオフライン再計算、PredictV3+api_predict改修(ローカル) | オフライン検証レポート |
| Day 4 | api_v3_shadow.php+Actions追加、シャドウ開始 | シャドウ稼働 |
| Day 5-11 | シャドウ観察(1週間)。日次で的中率をv2実績と比較 | 判定データ |
| Day 12 | 昇格判定→OK なら require切り替えデプロイ | v3本番化 |

**実働約4日+シャドウ1週間。** Day1-3は本番に影響ゼロ(読み取り専用+ローカル)、Day4のシャドウも本番テーブル・表示に不干渉。

### リスクと対策

| リスク | 対策 |
|---|---|
| ホスティングのメモリ/実行時間制限でexport失敗 | 2週間チャンク設計(§3.1)。落ちたら1週間に細分化 |
| リーク修正でオフライン指標が現行v2より見かけ悪化し判断に迷う | v2ベースラインも**リーク修正済みデータで再評価**して同一土俵比較(§3.2) |
| recent10クエリの本番負荷 | IN句1本+索引で軽微見込み。シャドウ期間中にapi_predict応答時間を計測 |
| 10月の新ファンデータでas-of前提が変わる | as-of JOINを今回から実装済みにしておく(§2.4) |

---

## 6. 決めていただきたいこと

1. この設計で実装に進んでよいか(特に: 特徴量21個の構成、シャドウ1週間、predictions_v2転用)
2. export_lr_data_v3.phpを検証後も残すか(月次再学習の道具になる) / 都度削除か
3. Day1の着手タイミング
