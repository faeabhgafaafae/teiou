# 艇王 v2予測モデル・戦略改善 分析レポート

作成日: 2026-09-03 / 分析範囲: predict_v2_core.php・run_lr.py・generate_strategies.php・直近30日実績(DB読み取りのみ)

---

## 0. 要約

- 現行v2(LR)は実質 **「枠番 + 当地勝率」の2特徴量モデル**。11特徴量中7つはほぼ死んでいる。
- DBには **未使用の強力な特徴量**(平均ST・コース別成績・直近成績)が既に存在する。モデル構造変更より先にこれを入れるべき。
- 学習データが **3週間分しかない**(2026-06-29〜07-19)。beforeinfoバックフィル済みの現在は2.5ヶ月分使える。再学習だけで改善が見込める。
- 的中特化の目標48.8%は **上位3艇ボックス6点という買い方のままでは構造的に到達不可能**。買い目フォーメーション変更か「見送り(レース選別)」の導入が必須。
- バランスはv2昇格後(8/28以降)に **的中率が上がったのに回収率が急落**(76.2%→64.9%)。オッズ上限25倍フィルタがv2と噛み合っていない。要再調整。
- 一撃重視はv2昇格後 **的中13.0%・回収105.5%で目標を既に超過**(サンプル912レースで参考値)。現設定維持を推奨。

---

## 1. 現行モデルの特徴量と予測力

### 1.1 パイプライン全体像

```
api_predict.php ──> PredictV2::score_race() ──> predictions.predicted_rank (v2_lr)
                                                      │
generate_strategies.php <─────────────────────────────┘
  ├ 的中特化: 上位3艇ボックス(6点)
  ├ バランス: 上位2艇1着固定×上位4艇流し(≦12点、オッズ25倍超除外)
  ├ 一撃重視: 1位固定×2-4位流し(≦6点、オッズ15倍未満除外)
  └ 絞り込み: 上位3艇を枠番昇順に並べた1点
```

- モデル: ロジスティック回帰(1着=1の2値分類)、係数はPHP定数として焼き込み
- 学習: 2026-07-28実施、Train期間 **2026-06-29〜07-19(3週間)**、Test AUC 0.8505
- 8/27にv1スコア方式から昇格(model_version='v2_lr')

### 1.2 係数(標準化後)による重要度

| 特徴量 | 係数 | 評価 |
|---|---|---|
| lane(枠番) | **-1.133** | 支配的。ただし線形扱いが不適切(後述) |
| local_win_rate(当地勝率) | **+0.975** | 2番目に強い。有効 |
| exhibit_time_rel(展示タイム相対) | +0.216 | 有効だが小さい |
| global_win_rate(全国勝率) | +0.148 | 当地勝率と共線、弱い |
| global_2rate(全国2連率) | -0.102 | **符号が逆転**(勝率と多重共線性の兆候) |
| start_timing(展示ST) | -0.089 | 弱い(速い=小さい値→負は正しい方向だが小さすぎる) |
| motor_2rate_rel(モーター相対) | +0.087 | 弱い |
| wave_height(波高) | +0.049 | ほぼノイズ。符号も直感と逆 |
| wind_speed(風速) | -0.037 | ほぼノイズ |
| local_2rate(当地2連率) | -0.018 | 死んでいる |
| temperature(気温) | -0.0001 | 完全に死んでいる |

**結論: 実質「枠番+当地勝率+展示タイム少々」のモデル。** 気象3特徴量は線形主効果では予測力ゼロ(風の影響は「向い風×ダッシュ勢有利」のような枠番との交互作用で現れるため、線形単独項では消える)。2連率系2つは勝率との共線性で係数が壊れており、ノイズ源になっている。

### 1.3 構造的な問題点

1. **枠番を連続値として線形扱い**: 実際の1着率は 1号艇≈55% → 2号艇≈14% → 3号艇≈12%…と急減衰する非線形。1枠→2枠の落差と5枠→6枠の落差を同じ傾きで表現しており、1号艇の過小評価・中枠の歪みが出る。→ one-hot化(6ダミー)で即解消。
2. **行単位2値分類+事後正規化**: 「同レース内で誰が勝つか」という相対問題を、レース文脈なしの絶対確率で解いてから合計1に正規化している。条件付きロジット(レース内softmax)が本来の定式化。**どちらも線形モデルなのでPHP側は定数差し替え+数行変更で済む。**
3. **学習データ3週間分**: 約2,500レース。過学習はしていない(train/test差なし)が、季節性も会場差も学べない量。backfill_beforeinfoで6月末〜9月分が揃った今、**再学習するだけで無料の改善**が得られる。
4. **再学習の仕組みがない**: 7/28の係数で凍結。モーター周期(半年)・期別成績更新・季節変化に追従できない。
5. **P(1着)しかモデリングしていない**: 2〜6位は1着確率のソート順。バランスの2-3着流しや絞り込みの並び順に必要な「2着・3着の構造」が無視されている(絞り込みで枠番順ソートというヒューリスティックに頼っているのはこの穴の証左)。

---

## 2. 未使用特徴量の提案(DB既存カラム優先)

### ★★★ 最優先(DBに既にあり、ドメイン的に強いことが既知)

| 特徴量 | ソース | 根拠 |
|---|---|---|
| **平均ST** (+レース内ST順位) | `player_periods.avg_st` | スタート力は1着率の最強予測因子の一つ。現行は展示ST(単走・当日1回)しか見ていない |
| **コース別成績** | `player_periods.c{1-6}_rank1/c{1-6}_count/c{1-6}_fukusho` | 「この選手がこの枠で走ったときの実績」。v1では使っていたのにv2で落ちている。枠番×選手の交互作用そのもの |
| **直近成績トレンド** | `results`から直近10走の平均着順・1着率 | 期別成績(最大6ヶ月前の集計)より鮮度が高い。節間の調子を捉える |
| **級別** | `players.grade` (A1/A2/B1/B2) | 勝率と相関するが、A1はグレードレース経験など勝率に出ない情報を含む |

### ★★ 有望(要検証)

| 特徴量 | ソース | 備考 |
|---|---|---|
| 直近の実戦ST平均・分散 | `results.start_timing` | 展示STより実戦ST。分散はF持ちの慎重スタート検出にも |
| 節間成績 | `results`(同一開催内の前走) | 当地×当節のモーター・水面適性の実測値 |
| 進入予定コース | `entries.exhibit_course` | 枠なり崩れ(前づけ)をモデルに伝える唯一の手段。枠番と乖離したときに効く |
| 風向×枠番の交互作用 | `races.wind_dir` | 風速単独では死ぬが「追い風→まくり有利」等は交互作用として効く |
| モーター2連率の絶対値 | `entries.motor_2rate` | 現行は相対値のみ。40%と60%のレース間差が消えている |
| F数 | `player_periods.c{1-6}_f` | F持ちはSTが遅くなる(慎重化)。avg_stと組み合わせ |
| 会場ダミーor会場別1号艇勝率 | `races.venue` | イン強弱の会場差(徳山・大村vs戸田・平和島)は既知で大きい |

### △ 注意が必要

- **単勝オッズ**(`entries.odds`): 市場の集合知で予測力は最強クラスだが、(a)取得タイミングと予測生成タイミングの整合、(b)「オッズを見てオッズに勝つ」構造の歪み、に注意。まずはモデル外(EVフィルタ側)で使うことを推奨。
- チルト・部品交換・体重: 予測力は小さい。優先度低。

---

## 3. モデル構造: LRの限界と移行判断

### 段階的移行を推奨(いきなりGBDTに行かない)

| 段階 | 内容 | 期待効果 | 実装コスト | デプロイ影響 |
|---|---|---|---|---|
| **Step A** | 枠番one-hot化+死に特徴量除去+特徴量追加(§2の★★★)+2.5ヶ月分で再学習 | AUC 0.850→0.87前後(見込) | run_lr.py改修+export_lr_data.php復活で **1〜2日** | PHP定数差し替えのみ |
| **Step B** | 条件付きロジット(レース内softmax)化+月次自動再学習(GitHub Actions) | 確率のキャリブレーション改善(EVフィルタの前提) | **1日** | score_race()を数行変更 |
| **Step C** | LightGBM(勾配ブースティング)移行 | AUC +0.01〜0.03上乗せ(見込) | **2〜4日** | 要アーキテクチャ判断(下記) |

### GBDT移行のメリット・デメリット

**メリット**: 非線形・交互作用(風×枠、モーター×会場等)の自動学習 / 欠損値ネイティブ対応(中央値補完不要) / feature importanceで継続的な特徴量監査。

**デメリット・コスト**:
- **PHPで直接推論できない**のが最大の障壁。選択肢は2つ:
  1. **m2cgen等でツリーをPHPコードに変換して焼き込み** — 現アーキテクチャ維持。モデル更新のたびに生成コード差し替え
  2. **GitHub ActionsのPythonで日次バッチ予測→import用エンドポイントにPOST** — 既にActions→api_v2_batch.phpの導線があるので自然な拡張。ただしリアルタイム性(直前情報更新後の再予測)が失われる点に注意
- 現データ量(〜1万レース)では**特徴量追加の効果 >> モデル構造変更の効果**。LRで特徴量を固めてから移行する方が、改善の切り分けもできて安全。
- キャリブレーションがLRより悪化しがち→EVフィルタ導入時はPlatt/isotonic補正が必要。

**判断: Step A/Bで頭打ちを確認してからStep Cへ。** LRのまま到達できる水準をまず取り切る。

---

## 4. 戦略別 買い目ロジックの検証

### 実績データ(strategy_stats.php集計)

| 戦略 | 直近30日(8/4-9/3) | v2昇格後(8/28-9/3, 912R) | 目標 |
|---|---|---|---|
| 的中特化 | 18.4% / 回収78.2% | 21.3% / **88.8%** | 48.8% / 83.5% |
| バランス | 25.7% / 76.2% | 30.2% / **64.9%⚠** | 42.0% / 83.8% |
| 一撃重視 | 10.0% / 76.8% | **13.0% / 105.5%✅** | 10.3% / 79.6% |
| 絞り込み | 7.2% / 88.1% | 8.8% / 83.2% | 9.9% / 89.5% |

### 4.1 的中特化 — **買い方の構造変更が必須**

上位3艇ボックス6点の的中=「3着以内の3艇を完全に当てる」。v2昇格後でも21.3%。**モデルが完璧に近づいても30〜35%程度が天井**で、48.8%には届かない。BOATERSの48.8%は(a)レース選別(自信のあるレースのみ提供)か(b)より広いフォーメーション(3着4艇目まで含む8〜12点等)のはず。

**提案**: 確信度連動2段構え
- 1位確率が閾値以上(例: 正規化後45%超) → 現行ボックス6点
- 閾値未満 → 「1-2位を1着固定×上位4艇2着×上位5艇3着」等の広いフォーメーション、または見送り
- ※目標値の測り方(BOATERS側の分母にレース選別が入っていないか)を一度確認する価値あり

### 4.2 バランス — **v2昇格後の回収率急落。オッズ上限25倍が主犯の疑い**

v2昇格後、的中率は25.7→30.2%に改善した一方、回収率は76.2→64.9%に急落。点数は名目12点に対し実質平均4.7点(オッズ上限フィルタが大量に枝刈り)、的中時平均払戻は約1,015円(≈10倍)。**「25倍超を一律除外」は、高確率低配当の-EV組み合わせを残し、中配当の+EV組み合わせを削る**方向に働いている。

**提案**: simulate_ichigeki.phpと同型の**読み取り専用simulate_balance.php**を作り、(a)上限15/25/40/60/なし、(b)EVフィルタ(モデル確率×オッズ≧閾値)への置換、を直近60日で比較。EVフィルタ版が本命(§5参照)。

### 4.3 一撃重視 — **現設定維持**

8/27変更(プール2-4位化+下限15倍)は読み取り専用シミュレーションの結果とも整合:

| プール | 下限 | 的中率 | 回収率 |
|---|---|---|---|
| 2-4位 | 30倍 | 5.9% | 96.8% |
| 2-4位 | 25倍 | 6.7% | 94.7% |
| **2-4位** | **15倍(現行)** | **11.0%** | **91.8%** |
| 3-5位 | 15倍 | 9.6% | 84.7% |
| 4-6位(旧) | 15倍 | 6.3% | 62.4% |

下限を25〜30倍に上げると回収率は上がるが的中率が目標(10.3%)を割る。**的中率・回収率の両立点として現行の15倍が最適**。実績(13.0%/105.5%)が目標超過なのでいじらない。

### 4.4 絞り込み — 目標まで僅差。モデル改善で自然到達見込み

8.8% vs 目標9.9%。1点買いの的中率はモデル精度の直接関数なので、§1-2のモデル改善で埋まる公算大。加えて「枠番昇順ソート」は1-2-3型に最適化されたヒューリスティックであり、**2着・3着モデル(Step B以降)ができたら「最尤順列」選択に置換**するとさらに伸びる余地。

---

## 5. 優先順位: 的中率と回収率を「両方」上げる施策

原則: **モデル精度向上**(同じ買い方で当たりが増える=両方↑)と**EVフィルタ**(買う組み合わせの質を上げる=回収率↑・的中率維持)と**レース選別**(当たりやすいレースに絞る=両方↑、本命サイドの過小人気傾向も追い風)は両立施策。オッズ下限の引き上げ(的中率↓)やプール拡大単独(回収率↓)はトレードオフ施策なので採らない。

| 優先 | 施策 | 的中率 | 回収率 | コスト | 対象戦略 |
|---|---|---|---|---|---|
| **P1** | 学習データ2.5ヶ月分で再学習+月次自動再学習 | ↑ | ↑ | 0.5日 | 全戦略 |
| **P2** | 特徴量追加(avg_ST・コース別成績・直近10走・級別)+死に特徴量整理 | ↑↑ | ↑ | 1〜2日 | 全戦略 |
| **P3** | 枠番one-hot+条件付きロジット化 | ↑ | ↑ | 1日 | 全戦略 |
| **P4** | simulate_balance.php(読み取り専用)でオッズ上限再調整/EVフィルタ比較 | → | ↑↑ | 1日 | バランス |
| **P5** | EVベース買い目フィルタ(odds_3t×Harville確率、p下限+EV下限の2段) | → | ↑↑ | 1〜2日 | バランス・的中特化 |
| **P6** | 的中特化の確信度連動フォーメーション(見送りor拡張) | ↑↑ | ↑ | 1〜2日(要シム) | 的中特化 |
| **P7** | LightGBM移行(Step A/B頭打ち確認後) | ↑ | ↑ | 2〜4日 | 全戦略 |
| **P8** | 2着・3着モデル→絞り込みの最尤順列化 | ↑ | ↑ | 2日 | 絞り込み・バランス |

**P1〜P3だけでLRのまま到達可能な水準を取り切る**のが今月の推奨スコープ。P4は読み取り専用でリスクゼロなので並行実施可。バランスの回収率64.9%は放置すると目標から一番遠いので、P4の緊急度が高い。

---

## 6. 次のアクション候補(意思決定待ち)

1. export_lr_data.php(git履歴 b4b30d3^ に残存)を復活させ、6-9月データでStep A再学習の効果測定 → 数字を見てから本番反映判断
2. simulate_balance.php を simulate_ichigeki.php をひな型に新規作成(読み取り専用) → オッズ上限とEVフィルタの比較データ取得
3. BOATERSの目標値の分母定義(レース選別の有無)の確認
