# v3wシャドウテスト中間分析 (2026-09-13〜09-17、判定日9/27の折り返し)

作成日: 2026-09-18
対象: v3w (重み付き学習 alpha=0.55、`predict_v3_core.php`) シャドウ開始2026-09-13〜09-17の5日分
データ: `shadow_eval_v3.php`(本番predictions_v2/predictionsの実測、839R) +
ローカル再現シム `sim_v3w_midterm.py`(`data/`のCSV、775R)
判定基準: `design_v3_model_20260903.md` §4.2 の基準①②③
(③は `design_v3_improvement_20260913.md` §2.3 のCI版hard gate: 全戦略 P(候補≥v2) ≥ 0.25)

## 0. データの層と信頼度(先に明記)

| 層 | ソース | 用途 | 注意 |
|---|---|---|---|
| A | shadow_eval_v3.php 実測 | 基準①②のtop1的中率(確定値) | 839R。predictions_v2/predictionsの実登録値 |
| B | 本番戦略実績 strategy_prod_v2 | 基準③の「現行実績」側 | 現行ロジック+prob傾斜配分の実運用値 |
| C | ローカル同一ロジックシム | 基準③のモデル効果分離+bootstrap gate | 均等100円。**v2側はオフライン近似で本番v2より弱い**(後述) |

**留保(重要)**: (1) 5日775〜839Rは判定予定期間(2週間・約2,200R)の約4割にすぎず、
ROIは少数の高配当的中に支配されるため以下の数値は大きく動きうる。
(2) ローカル再現のv2はtop1 49.5%と本番v2実測(59.0%)を約10pt下回る。これは
walk-forward時代から知られたオフライン/本番経路の乖離(オフラインv2 49.3% vs
シャドウ実測v2 56.8%)と同型で、**層Cのv2側は本番v2より甘い比較対象**である。
v3w側はローカル57.5% vs 実測56.5%でほぼ一致しており、再現は信頼できる。
(3) 09-18分は結果未確定のため含まない。

---

## 1. 基準①: 1着的中率の推移 と 基準②: オフライン推定55.4%との乖離

shadow_eval_v3.php 実測(839R):

| 日付 | v2(本番) | v3w(シャドウ) | 勝敗 |
|---|---|---|---|
| 09-13 | 60.6% (109/180) | 57.2% (103/180) | v2 |
| 09-14 | 62.3% (104/167) | 53.3% (89/167) | v2 |
| 09-15 | 57.1% (89/156) | 53.2% (83/156) | v2 |
| 09-16 | 68.6% (107/156) | 60.3% (94/156) | v2 |
| 09-17 | 47.8% (86/180) | 58.3% (105/180) | **v3w** |
| **累計** | **59.0%** | **56.5%** | v2 +2.5pt |

- **基準①(v3w > v2): 中間時点で未達**(−2.5pt)。5日中4日でv2が勝ち、v3wが上回ったのは
  09-17のみ。
- ただしv2の59.0%は、前回シャドウ期間(9/4-9/12)の実測56.8%を+2.2pt上回る**上振れ週**。
  v2側の平均回帰だけで差の大半が縮む余地はある(逆に言えば、v3wが実力で上回っている
  証拠も現時点ではない)。
- **基準②(オフライン推定−3pt以内): 達成**。v3w実測56.5% vs オフライン推定55.4%で
  **+1.1pt**。実装バグ・リーク残りを疑う乖離はない。むしろ推定を上回っており、
  export→学習→PHP移植の経路は健全。

## 2. 1号艇rank1率(本命寄り度)

ローカル再現(775R、v3w側はtop1がAPI実測と1pt以内で一致):

| 指標 | 値 | 参照値 |
|---|---|---|
| v3w 1号艇rank1率 | **78.8%** | 設計想定77.0%(オフライン) |
| (参考)ローカルv2再現 | 62.5% | v2履歴値64.5% |
| 旧v3(重みなし) | ─ | 91.7% |
| 実際の1号艇1着率(期間内) | 55.9% | 歴史値約54% |

- **想定どおり**。重み付き学習の狙い(91.7%→77%への緩和)は本番データ経路でも
  78.8%とほぼ設計値どおりに再現された。
- 実勝率+22.9ptで、soft警告基準(実勝率+15pt)には抵触するが、これは設計時に
  hard→soft降格を決めた際の想定内(ROI実害はhard gateが直接測る)。

## 3. 基準③: 4戦略ROI

### 3.1 本番実績(層B: 現行v2 + prob傾斜配分、839R)

| 戦略 | 的中率 | ROI |
|---|---|---|
| 的中特化 | 39.2% | 93.8% |
| バランス | 44.5% | 98.7% |
| 一撃重視 | 13.1% | **118.1%** |
| 絞り込み | 19.8% | 109.7% |

今週の本番v2は一撃118%・絞り込み110%と**例外的に強い週**(全戦略が90%超)。
この実績を「悪化させない」のはどのモデルにも困難な高いバーである点に注意。

### 3.2 同一ロジック比較(層C: 現行買い目ロジック・均等100円、775R)

| 戦略 | v2(オフライン近似) | v3w | ROI差 | P(v3w≥v2) | hard gate |
|---|---|---|---|---|---|
| 的中特化 | 36.8% / 79.7% | 42.7% / 82.2% | **+2.6pt** | **0.644** | 合格 |
| バランス | 41.6% / 86.4% | 44.6% / 79.1% | −7.3pt | **0.115** | **不合格** |
| 一撃重視 | 11.7% / 93.2% | 10.9% / 85.0% | −8.2pt | 0.302 | 合格(辛勝) |
| 絞り込み | 16.5% / 85.3% | 19.4% / 83.3% | −2.0pt | 0.420 | 合格 |

- **的中特化はv3wが的中率+5.9pt・ROI+2.6ptで明確に優位**(P=0.644)。オフライン
  検証(v3w05で86.0≥85.7)と同じ構図が本番経路でも再現。
- **バランスがhard gate不合格**(P=0.115)。オフラインでは−1.4pt(ノイズ帯域)だった
  悪化が−7.3ptに拡大。要経過観察の最重要項目。
- **一撃重視**: 点推定−8.2ptだが分散が大きくP=0.302で辛うじて合格。旧v3のような
  崩壊(オフライン−9.6pt・P=0.074)とは異なり、「劣勢だが確定的でない」水準。
  なお層Cのv2側は本番v2より弱いため、本番v2(118.1%)との実力差は
  この表の見かけより大きい可能性が高い。
- shadow_eval_v3.php内蔵の戦略シム(box6的中特化・絞り込み1点の**旧ロジック**)は
  73.5〜83.7%と低く出るが、現行本番ロジックとの直接比較には使えない(参考値)。

## 4. 9/27昇格判定の見込みと打ち手

### 4.1 見込み評価

| 基準 | 中間時点 | 9/27見込み |
|---|---|---|
| ① top1がv2超え | ✗ (−2.5pt) | **不透明**。v2の上振れ(59.0%)の平均回帰頼み。v3w自力での逆転根拠は現状ない |
| ② オフライン推定−3pt以内 | ✓ (+1.1pt) | **達成濃厚**。乖離は安定して小さい |
| ③ 4戦略ROI非悪化(CI版) | ✗ (バランスP=0.115) | **全戦略一括では困難**。的中特化のみ明確合格、バランスは回復が必要 |

**総合: 全戦略一括昇格(①②③すべて充足)は現時点の見込みでは難しい。**
①はv2上振れとの相対評価で残り1週間の変動幅内だが、③のバランス悪化(−7.3pt)が
このまま残ると一括昇格は基準上不可能。

### 4.2 残り約1週間で取れる打ち手(推奨順)

1. **alpha再調整はしない(現行0.55のまま9/26まで走り切る)**。
   理由: (a) シャドウ途中でモデルを差し替えるとpredictions_v2に新旧予測が混在し、
   9/4-9/12の旧v3データと同じ「評価不能期間」を作る。再差し替え後は実質8日分しか
   取れず、今回と同じサンプル不足に陥る。(b) グリッドサーチ済みの範囲に妙手がない:
   alphaを下げる(0.4)と旧v3方向=バランス・一撃のROI悪化が深刻化、上げる(0.7)と
   絞り込みROIが崩壊することが判明済み。0.55は既にパレート上の選択。
2. **9/27判定を「戦略別判定」として運用する準備を進める**(設計書§6の段階的
   ハイブリッド昇格の実行)。中間データは第1段「的中特化のみv3wへ切替」を
   明確に支持している(的中率+5.9pt・ROI+2.6pt・P=0.644)。具体的には
   generate_strategies.php の戦略タイプ→参照テーブル(predictions/predictions_v2)の
   マップ実装を判定日前に用意しておき、9/27に「①②を満たさなくても
   的中特化の③単独合格なら第1段昇格」と判定できるようにする。
   ※本書は分析メモであり、この実装自体は別途判断・別コミットで行う。
3. **判定材料の精緻化**: 9/27の判定時に (a) 全2週間分でbootstrap gateを再計算、
   (b) 層Cを均等100円だけでなく本番同等のprob傾斜配分でも再計算、
   (c) v2上振れの寄与を除くため「同一レース・同一ロジック」比較を主表にする。
   sim_v3w_midterm.py がそのまま流用可能(DATESを2週間分に広げるだけ)。
4. **バランス悪化の原因切り分け**(判定までに1回): v3wのバランス構成
   (top2×top4流し)がオッズ100倍フィルタとどう相互作用しているかを
   レース単位で確認する。旧v3の一撃崩壊と同じ「本命寄りランクと
   高配当フィルタの衝突」がバランス側で起きている可能性がある。
   仮にそうなら、alphaではなく**バランスのみv2維持**(ハイブリッド構成の
   対象戦略調整)が正しい対処になる。

### 4.3 結論

- ②は健全、①は劣勢(ただしv2上振れ週との比較)、③はバランスが明確に足を引く。
- **全戦略一括昇格は見送りの公算が大きい。設計書§6どおり「的中特化のみの
  第1段ハイブリッド昇格」を9/27のメインシナリオとして準備することを推奨する。**
- いずれの判断も残り1週間・約2倍のサンプルで確定させる。本メモの数値は
  中間値であり、特にROI系は変動幅が大きい。

---

## 付録

- 分析スクリプト: `sim_v3w_midterm.py`(読み取り専用・DB接続なし。data/のCSVのみ使用)
- 実測ソース: `shadow_eval_v3.php?from=2026-09-13&to=2026-09-18`(09-18は未確定のため実質09-17まで)
- 関連: `design_v3_model_20260903.md` §4.2(基準定義)、
  `design_v3_improvement_20260913.md`(CI版gate・alpha=0.55採用経緯)、
  `v3w055_training_output.txt`(オフライン推定55.4%)
